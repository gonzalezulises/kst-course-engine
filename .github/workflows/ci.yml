name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Create venv and install dependencies
        run: uv venv --python ${{ matrix.python-version }} && uv pip install -e ".[dev]"

      - name: Lint (ruff check)
        run: uv run ruff check kst_core/ tests/

      - name: Format (ruff format)
        run: uv run ruff format --check kst_core/ tests/

      - name: Type check (mypy)
        run: uv run mypy kst_core/

      - name: Test (pytest)
        run: uv run pytest --cov-fail-under=100
